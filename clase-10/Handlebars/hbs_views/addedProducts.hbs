<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carga de productos</title>
</head>
<body>
  <h1>Productos agregados con Handlebars</h1>
  {{#if productos}}
  <table>
    <thead>
      <th>Nombre</th>
      <th>Precio</th>
      <th>Imagen</th>
    </thead>
    <tbody>
      {{#each productos}}
        <td>{{title}}</td>
        <td>{{price}}</td>
        <td><img src="{{thumbnail}}" alt={{title}}></td>
      {{/each}}
    </tbody>
  </table>
  {{else}}
  <h2>No hay productos en la base de datos</h2>
  {{/if}}
</body>
</html>